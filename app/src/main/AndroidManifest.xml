<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.convertidorclase">
    <uses-permission android:name="android.permission.CAMERA"></uses-permission>
    <uses-feature android:name="android.hardware.Camera"></uses-feature>
    <uses-permission android:name="android.permission.FLASHLIGHT"></uses-permission>
    <uses-feature android:name="android.hardware.Camera.flash"></uses-feature>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".area_Activity"></activity>
        <activity android:name=".Codigomorse_Activity" />
        <activity android:name=".Longitud_Activity" />
        <activity android:name=".Frecuencia_Activity" />
        <activity android:name=".ConsumoDeCombustible_Activity" />
        <activity android:name=".Energia_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>